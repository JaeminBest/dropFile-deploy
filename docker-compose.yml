version: '3.0'

services:
  web:
    build: .
    ports:
      - "8080:8080"
      - "80:80"
      - "8000:8000"
    env_file:
      - ./.env
    restart: "always"
    volumes:
      - .:/home
      - ./storage:/storage
